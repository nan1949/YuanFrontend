import React from 'react';

const Header = () => {
  return (
    <header class="fixed top-0 w-full h-10 border-b border-gray-200 border-solid bg-white z-50">
      {/* Header 内容以后再添加 */}
    </header>
  );
};

export default Header;